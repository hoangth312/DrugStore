package com.hoanganh.drugstore.model.product

data class EvaluateInit(val comment: String,
                        val productId: Int,
                        val userId: Int
                        )